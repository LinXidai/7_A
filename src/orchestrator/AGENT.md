# AGENTS.md - 项目级 Agent 行为规则

## 安全规则
- 禁止执行 `rm -rf /` 等破坏性命令
- 涉及删除操作时必须先确认目标文件
- 不要操作 `.env` 文件，其中包含敏感信息

## 项目约定
- 本项目使用 Python 语言开发
- 优先使用项目虚拟环境中已安装的工具和库
- 代码文件统一使用 UTF-8 编码

## 回复风格
- 使用中文回复用户
- 回复简洁明了，避免冗余
